"""Dashboard section generators — charts.py, cards.py, predictions.py.

Color palette constants shared across all section modules.
"""

# ── Safety palette (evaluative) ──
SAFE = "#34d399"
CAUTION = "#fbbf24"
DANGER = "#f87171"

# ── 5-zone intensity palette ──
Z1 = "#93c5fd"   # Recovery  (blue-300)
Z2 = "#60a5fa"   # Easy      (blue-400)
Z3 = "#e9b308"   # Moderate  (warm yellow — distinct from CAUTION)
Z4 = "#f97316"   # Hard      (orange-400)
Z5 = "#ef4444"   # Very Hard (red-400)

# ── Accent ──
ACCENT = "#818cf8"
